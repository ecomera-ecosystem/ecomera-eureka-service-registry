name: Ecomera Eureka Server CI

on:
  workflow_dispatch: #
  push:
    branches: [ "master", "development" ]
    paths:
      - "java/**"
      - "pom.xml"
      - "ci.yml"
  pull_request:
    branches: [ "master", "development" ]
    paths:
      - "java/**"
      - "pom.xml"
      - "ci.yml"

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build & test
        run: mvn -B clean verify

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: test-results
          path: target/surefire-reports/*.xml

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v7.0.0
        with:
          name: ecomera-eureka-service-registry-${{ github.sha }}
          path: target/*.jar
          retention-days: 90